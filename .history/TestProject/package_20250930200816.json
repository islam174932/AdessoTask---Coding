{
  "name": "testproject",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "smoke": "cucumber-js --require features/step_definitions/smoke.steps.ts --tags @smoke features/*.feature",
    "regression": "cucumber-js --require features/step_definitions/regression.steps.ts --tags @regression features/*.feature",
    "test": "npm run smoke && npm run regression",
    "test:parallel": "npm-run-all --parallel smoke regression",
    "smoke:headless": "cross-env HEADLESS=true npm run smoke",
    "regression:headless": "cross-env HEADLESS=true npm run regression",
    "test:headless": "cross-env HEADLESS=true npm run test",
    "test:parallel:headless": "npm-run-all --parallel smoke:headless regression:headless",
    "smoke:chrome": "cross-env BROWSER=chromium npm run smoke",
    "smoke:firefox": "cross-env BROWSER=firefox npm run smoke",
    "smoke:webkit": "cross-env BROWSER=webkit npm run smoke",
    "regression:chrome": "cross-env BROWSER=chromium npm run regression",
    "regression:firefox": "cross-env BROWSER=firefox npm run regression",
    "regression:webkit": "cross-env BROWSER=webkit npm run regression",
    "smoke:chrome:headless": "cross-env BROWSER=chromium HEADLESS=true npm run smoke",
    "smoke:firefox:headless": "cross-env BROWSER=firefox HEADLESS=true npm run smoke",
    "smoke:webkit:headless": "cross-env BROWSER=webkit HEADLESS=true npm run smoke",
    "regression:chrome:headless": "cross-env BROWSER=chromium HEADLESS=true npm run regression",
    "regression:firefox:headless": "cross-env BROWSER=firefox HEADLESS=true npm run regression",
    "regression:webkit:headless": "cross-env BROWSER=webkit HEADLESS=true npm run regression",
    "test:all-browsers": "npm run smoke:chrome && npm run smoke:firefox && npm run smoke:webkit && npm run regression:chrome && npm run regression:firefox && npm run regression:webkit"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^9.0.0",
    "@playwright/test": "^1.0.0",
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5",
    "playwright": "^1.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
